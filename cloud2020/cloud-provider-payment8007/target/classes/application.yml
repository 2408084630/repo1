server:
  port: 8007
spring:
  application:
    name: cloud-payment-service   #微服务名称
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}